# Production API (default if unset)
NEXT_PUBLIC_API_BASE_URL=https://handkerchief-apis.addonops.com/v1

# Local backend only — uncomment when running backend locally:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/v1
